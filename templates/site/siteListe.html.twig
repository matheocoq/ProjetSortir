{% extends 'base.html.twig' %}

{% block title %}
    Liste des sites
{% endblock %}

{% block body %}

<div class="div-liste-lieux">
<table class="table margin-30 tableau-liste-lieux">
    <thead class="thead-light">
        <tr>
            <th scope="col">Nom</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for site in sites %}
         <tr>
            <td>{{ site.nom}}</td>
            <td>
                <a class="text-decoration-none fw-bold link-primary margin-right" href="{{ path('site_update', {id: site.id}) }}">Modifier</a>
                <a class="text-decoration-none fw-bold link-primary" href="{{ path('site_delete', { 'id': site.id }) }}">Supprimer</a>     
            </td>
        </tr>
    {% endfor %}  
    </tbody>
</table>
</div>
<a class="btn btn-primary bouton-cree-lieux" href="{{ path('site_create')}}">Ajouter</a>
{% endblock %}